import styled from "styled-components";

 const Header = styled.header`
  background-color: #1f1f1b;
  padding: 10px 0;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 99999;
`
export const S = {
    Header
}